describe('Get all forms', () => {
  it('should respond with all form titles and ids', () => {
    // 
  })
})


describe('Get form get by id', () => {
  it('should accept only integer id as param', () => {
    // works
  })
  it('should return form title and id', () => {

  })
  it('should return form questions list with question text, type, is required', () => {

  })
  it('should return error message if form id does not exist', () => {

  })
})
